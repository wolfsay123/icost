# 智记项目协作规则

## 变更日志与回滚

- 每次修改代码、配置、依赖、数据结构、构建产物或功能文档，都必须在同一次提交中更新 `docs/CHANGELOG.md`。
- 每条变更记录必须写明日期、变更点、影响面、验证结果和回滚方式；未验证项必须明确标为待验证。
- 数据迁移、备份格式和 Android 版本变更必须额外说明向前兼容性，以及回滚前是否需要先备份用户数据。
- 一个提交只承载一组可独立说明和回滚的相关改动，禁止把无关修改混入同一提交。
- 提交或推送后必须报告提交 SHA，并确认本地 HEAD、跟踪分支与远端分支是否一致。
- 回滚优先使用 `git revert <commit-sha>` 保留历史；除非用户明确授权，不使用会改写历史或丢失工作区内容的命令。
